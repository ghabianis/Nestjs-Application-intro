/*
//------------------------------------------------------------------------------ 
// This code was generated by Amplication. 
// 
// Changes to this file will be lost if the code is regenerated. 
//
// There are other ways to to customize your code, see this doc to learn more
// https://docs.amplication.com/docs/how-to/custom-code
//
//------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { PaginatedInterface } from "../../util/PaginatedInterface";
import { FollowEntrepriseService } from "../followEntreprise.service";
import { FollowEntrepriseCreateInput } from "./FollowEntrepriseCreateInput";
import { FollowEntrepriseWhereInput } from "./FollowEntrepriseWhereInput";
import { FollowEntrepriseWhereUniqueInput } from "./FollowEntrepriseWhereUniqueInput";
import { FollowEntrepriseFindManyArgs } from "./FollowEntrepriseFindManyArgs";
import { FollowEntrepriseUpdateInput } from "./FollowEntrepriseUpdateInput";
import { FollowEntreprise } from "./FollowEntreprise";
import { Post } from "../../post/base/Post";
import { getListFollowEntrepriseDto } from "./getListFollowEntreprise.dto";
import { UserData } from "src/auth/gqlUserData.decorator";
import { User } from "src/user/base/User";
@swagger.ApiBearerAuth()
export class FollowEntrepriseControllerBase {
  constructor(
    protected readonly service: FollowEntrepriseService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "FollowEntreprise",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: FollowEntreprise })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: FollowEntrepriseCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[],
    @UserData() userData: User
  ): Promise<FollowEntreprise> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "FollowEntreprise",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"FollowEntreprise"} creation is forbidden for roles: ${roles}`
      );
    }
         console.log(userData,"userDatauserDatauserDatauserData");
         
    return await this.service.create({
      data: {
        ...data,

        entreprise: data.entreprise
          ? {
              connect: data.entreprise,
            }
          : undefined,

        candidate: userData.candidateId 
          ? {
              connect: {id : userData.candidateId},
            }
          : undefined,
      },
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,

        entreprise: {
          select: {
            id: true,
          },
        },

        candidate: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "FollowEntreprise",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: getListFollowEntrepriseDto })
  @swagger.ApiForbiddenResponse()
  @swagger.ApiQuery({
    type: () => FollowEntrepriseFindManyArgs,
    style: "deepObject",
    explode: true,
  })
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<PaginatedInterface<FollowEntreprise>> {
    const args = plainToClass(FollowEntrepriseFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "FollowEntreprise",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,

        entreprise: {
          select: {
            id: true,
          },
        },

        candidate: {
          select: {
            id: true,
          },
        },
      },
    });
    const result = results.paginatedResult.map((result: FollowEntreprise) =>
      permission.filter(result)
    );
    return { paginatedResult: result, totalCount: results.totalCount };
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "FollowEntreprise",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: FollowEntreprise })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: FollowEntrepriseWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<FollowEntreprise | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "FollowEntreprise",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,

        entreprise: {
          select: {
            id: true,
          },
        },

        candidate: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "FollowEntreprise",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: FollowEntreprise })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: FollowEntrepriseWhereUniqueInput,
    @common.Body()
    data: FollowEntrepriseUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<FollowEntreprise | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "FollowEntreprise",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"FollowEntreprise"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          entreprise: data.entreprise
            ? {
                connect: data.entreprise,
              }
            : undefined,

          candidate: data.candidate
            ? {
                connect: data.candidate,
              }
            : undefined,
        },
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,

          entreprise: {
            select: {
              id: true,
            },
          },

          candidate: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "FollowEntreprise",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: FollowEntreprise })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: FollowEntrepriseWhereUniqueInput
  ): Promise<FollowEntreprise | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,

          entreprise: {
            select: {
              id: true,
            },
          },

          candidate: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
