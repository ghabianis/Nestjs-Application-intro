/*
//------------------------------------------------------------------------------ 
// This code was generated by Amplication. 
// 
// Changes to this file will be lost if the code is regenerated. 
//
// There are other ways to to customize your code, see this doc to learn more
// https://docs.amplication.com/docs/how-to/custom-code
//
//------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import * as gqlUserRoles from "../../auth/gqlUserRoles.decorator";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { PaginatedInterface } from "../../util/PaginatedInterface";
import { CreateSocialinterractionArgs } from "./CreateSocialinterractionArgs";
import { UpdateSocialinterractionArgs } from "./UpdateSocialinterractionArgs";
import { DeleteSocialinterractionArgs } from "./DeleteSocialinterractionArgs";
import { SocialinterractionFindManyArgs } from "./SocialinterractionFindManyArgs";
import { SocialinterractionFindUniqueArgs } from "./SocialinterractionFindUniqueArgs";
import { Socialinterraction } from "./Socialinterraction";
import { Publication } from "../../publication/base/Publication";
import { User } from "../../user/base/User";
import { SocialinterractionService } from "../socialinterraction.service";

@graphql.Resolver(() => Socialinterraction)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class SocialinterractionResolverBase {
  constructor(
    protected readonly service: SocialinterractionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Socialinterraction",
    action: "read",
    possession: "any",
  })
  async _socialinterractionsMeta(
    @graphql.Args() args: SocialinterractionFindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @graphql.Query(() => [Socialinterraction])
  @nestAccessControl.UseRoles({
    resource: "Socialinterraction",
    action: "read",
    possession: "any",
  })
  async socialinterractions(
    @graphql.Args() args: SocialinterractionFindManyArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<PaginatedInterface<Socialinterraction>> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Socialinterraction",
    });
    const results = await this.service.findMany(args);
    const result = results.paginatedResult.map((result: Socialinterraction) =>
      permission.filter(result)
    );
    return { paginatedResult: result, totalCount: results.totalCount };
  }

  @graphql.Query(() => Socialinterraction, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Socialinterraction",
    action: "read",
    possession: "own",
  })
  async socialinterraction(
    @graphql.Args() args: SocialinterractionFindUniqueArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Socialinterraction | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "Socialinterraction",
    });
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.Mutation(() => Socialinterraction)
  @nestAccessControl.UseRoles({
    resource: "Socialinterraction",
    action: "create",
    possession: "any",
  })
  async createSocialinterraction(
    @graphql.Args() args: CreateSocialinterractionArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Socialinterraction> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "Socialinterraction",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"Socialinterraction"} creation is forbidden for roles: ${roles}`
      );
    }
    // @ts-ignore
    return await this.service.create({
      ...args,
      data: {
        ...args.data,

        publication: args.data.publication
          ? {
              connect: args.data.publication,
            }
          : undefined,

        user: args.data.user
          ? {
              connect: args.data.user,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => Socialinterraction)
  @nestAccessControl.UseRoles({
    resource: "Socialinterraction",
    action: "update",
    possession: "any",
  })
  async updateSocialinterraction(
    @graphql.Args() args: UpdateSocialinterractionArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Socialinterraction | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "Socialinterraction",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"Socialinterraction"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      // @ts-ignore
      return await this.service.update({
        ...args,
        data: {
          ...args.data,

          publication: args.data.publication
            ? {
                connect: args.data.publication,
              }
            : undefined,

          user: args.data.user
            ? {
                connect: args.data.user,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Socialinterraction)
  @nestAccessControl.UseRoles({
    resource: "Socialinterraction",
    action: "delete",
    possession: "any",
  })
  async deleteSocialinterraction(
    @graphql.Args() args: DeleteSocialinterractionArgs
  ): Promise<Socialinterraction | null> {
    try {
      // @ts-ignore
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Publication, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Socialinterraction",
    action: "read",
    possession: "any",
  })
  async publication(
    @graphql.Parent() parent: Socialinterraction,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Publication | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Publication",
    });
    const result = await this.service.getPublication(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.ResolveField(() => User, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Socialinterraction",
    action: "read",
    possession: "any",
  })
  async user(
    @graphql.Parent() parent: Socialinterraction,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<User | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "User",
    });
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }
}
