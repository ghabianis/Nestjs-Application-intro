/*
//------------------------------------------------------------------------------ 
// This code was generated by Amplication. 
// 
// Changes to this file will be lost if the code is regenerated. 
//
// There are other ways to to customize your code, see this doc to learn more
// https://docs.amplication.com/docs/how-to/custom-code
//
//------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import * as gqlUserRoles from "../../auth/gqlUserRoles.decorator";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { PaginatedInterface } from "../../util/PaginatedInterface";
import { CreateQuestionTypeArgs } from "./CreateQuestionTypeArgs";
import { UpdateQuestionTypeArgs } from "./UpdateQuestionTypeArgs";
import { DeleteQuestionTypeArgs } from "./DeleteQuestionTypeArgs";
import { QuestionTypeFindManyArgs } from "./QuestionTypeFindManyArgs";
import { QuestionTypeFindUniqueArgs } from "./QuestionTypeFindUniqueArgs";
import { QuestionType } from "./QuestionType";
import { QuestionFindManyArgs } from "../../question/base/QuestionFindManyArgs";
import { Question } from "../../question/base/Question";
import { QuestionTypeService } from "../questionType.service";

@graphql.Resolver(() => QuestionType)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class QuestionTypeResolverBase {
  constructor(
    protected readonly service: QuestionTypeService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "QuestionType",
    action: "read",
    possession: "any",
  })
  async _questionTypesMeta(
    @graphql.Args() args: QuestionTypeFindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @graphql.Query(() => [QuestionType])
  @nestAccessControl.UseRoles({
    resource: "QuestionType",
    action: "read",
    possession: "any",
  })
  async questionTypes(
    @graphql.Args() args: QuestionTypeFindManyArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<PaginatedInterface<QuestionType>> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "QuestionType",
    });
    const results = await this.service.findMany(args);
    const result = results.paginatedResult.map((result: QuestionType) =>
      permission.filter(result)
    );
    return { paginatedResult: result, totalCount: results.totalCount };
  }

  @graphql.Query(() => QuestionType, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "QuestionType",
    action: "read",
    possession: "own",
  })
  async questionType(
    @graphql.Args() args: QuestionTypeFindUniqueArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<QuestionType | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "QuestionType",
    });
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.Mutation(() => QuestionType)
  @nestAccessControl.UseRoles({
    resource: "QuestionType",
    action: "create",
    possession: "any",
  })
  async createQuestionType(
    @graphql.Args() args: CreateQuestionTypeArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<QuestionType> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "QuestionType",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"QuestionType"} creation is forbidden for roles: ${roles}`
      );
    }
    // @ts-ignore
    return await this.service.create({
      ...args,
      data: args.data,
    });
  }

  @graphql.Mutation(() => QuestionType)
  @nestAccessControl.UseRoles({
    resource: "QuestionType",
    action: "update",
    possession: "any",
  })
  async updateQuestionType(
    @graphql.Args() args: UpdateQuestionTypeArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<QuestionType | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "QuestionType",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"QuestionType"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      // @ts-ignore
      return await this.service.update({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => QuestionType)
  @nestAccessControl.UseRoles({
    resource: "QuestionType",
    action: "delete",
    possession: "any",
  })
  async deleteQuestionType(
    @graphql.Args() args: DeleteQuestionTypeArgs
  ): Promise<QuestionType | null> {
    try {
      // @ts-ignore
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [Question])
  @nestAccessControl.UseRoles({
    resource: "QuestionType",
    action: "read",
    possession: "any",
  })
  async questions(
    @graphql.Parent() parent: QuestionType,
    @graphql.Args() args: QuestionFindManyArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Question[]> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Question",
    });
    const results = await this.service.findQuestions(parent.id, args);

    if (!results) {
      return [];
    }

    return results.map((result) => permission.filter(result));
  }
}
