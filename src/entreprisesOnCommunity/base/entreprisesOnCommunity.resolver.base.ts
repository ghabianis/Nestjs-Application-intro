/*
//------------------------------------------------------------------------------ 
// This code was generated by Amplication. 
// 
// Changes to this file will be lost if the code is regenerated. 
//
// There are other ways to to customize your code, see this doc to learn more
// https://docs.amplication.com/docs/how-to/custom-code
//
//------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import * as gqlUserRoles from "../../auth/gqlUserRoles.decorator";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { PaginatedInterface } from "../../util/PaginatedInterface";
import { CreateEntreprisesOnCommunityArgs } from "./CreateEntreprisesOnCommunityArgs";
import { UpdateEntreprisesOnCommunityArgs } from "./UpdateEntreprisesOnCommunityArgs";
import { DeleteEntreprisesOnCommunityArgs } from "./DeleteEntreprisesOnCommunityArgs";
import { EntreprisesOnCommunityFindManyArgs } from "./EntreprisesOnCommunityFindManyArgs";
import { EntreprisesOnCommunityFindUniqueArgs } from "./EntreprisesOnCommunityFindUniqueArgs";
import { EntreprisesOnCommunity } from "./EntreprisesOnCommunity";
import { Entreprise } from "../../entreprise/base/Entreprise";
import { Community } from "../../community/base/Community";
import { EntreprisesOnCommunityService } from "../entreprisesOnCommunity.service";

@graphql.Resolver(() => EntreprisesOnCommunity)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class EntreprisesOnCommunityResolverBase {
  constructor(
    protected readonly service: EntreprisesOnCommunityService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "EntreprisesOnCommunity",
    action: "read",
    possession: "any",
  })
  async _entreprisesOnCommunitiesMeta(
    @graphql.Args() args: EntreprisesOnCommunityFindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @graphql.Query(() => [EntreprisesOnCommunity])
  @nestAccessControl.UseRoles({
    resource: "EntreprisesOnCommunity",
    action: "read",
    possession: "any",
  })
  async entreprisesOnCommunities(
    @graphql.Args() args: EntreprisesOnCommunityFindManyArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<PaginatedInterface<EntreprisesOnCommunity>> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "EntreprisesOnCommunity",
    });
    const results = await this.service.findMany(args);
    const result = results.paginatedResult.map(
      (result: EntreprisesOnCommunity) => permission.filter(result)
    );
    return { paginatedResult: result, totalCount: results.totalCount };
  }

  @graphql.Query(() => EntreprisesOnCommunity, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "EntreprisesOnCommunity",
    action: "read",
    possession: "own",
  })
  async entreprisesOnCommunity(
    @graphql.Args() args: EntreprisesOnCommunityFindUniqueArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<EntreprisesOnCommunity | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "EntreprisesOnCommunity",
    });
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.Mutation(() => EntreprisesOnCommunity)
  @nestAccessControl.UseRoles({
    resource: "EntreprisesOnCommunity",
    action: "create",
    possession: "any",
  })
  async createEntreprisesOnCommunity(
    @graphql.Args() args: CreateEntreprisesOnCommunityArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<EntreprisesOnCommunity> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "EntreprisesOnCommunity",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"EntreprisesOnCommunity"} creation is forbidden for roles: ${roles}`
      );
    }
    // @ts-ignore
    return await this.service.create({
      ...args,
      data: {
        ...args.data,

        entreprise: args.data.entreprise
          ? {
              connect: args.data.entreprise,
            }
          : undefined,

        community: args.data.community
          ? {
              connect: args.data.community,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => EntreprisesOnCommunity)
  @nestAccessControl.UseRoles({
    resource: "EntreprisesOnCommunity",
    action: "update",
    possession: "any",
  })
  async updateEntreprisesOnCommunity(
    @graphql.Args() args: UpdateEntreprisesOnCommunityArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<EntreprisesOnCommunity | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "EntreprisesOnCommunity",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"EntreprisesOnCommunity"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      // @ts-ignore
      return await this.service.update({
        ...args,
        data: {
          ...args.data,

          entreprise: args.data.entreprise
            ? {
                connect: args.data.entreprise,
              }
            : undefined,

          community: args.data.community
            ? {
                connect: args.data.community,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => EntreprisesOnCommunity)
  @nestAccessControl.UseRoles({
    resource: "EntreprisesOnCommunity",
    action: "delete",
    possession: "any",
  })
  async deleteEntreprisesOnCommunity(
    @graphql.Args() args: DeleteEntreprisesOnCommunityArgs
  ): Promise<EntreprisesOnCommunity | null> {
    try {
      // @ts-ignore
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Entreprise, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "EntreprisesOnCommunity",
    action: "read",
    possession: "any",
  })
  async entreprise(
    @graphql.Parent() parent: EntreprisesOnCommunity,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Entreprise | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Entreprise",
    });
    const result = await this.service.getEntreprise(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.ResolveField(() => Community, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "EntreprisesOnCommunity",
    action: "read",
    possession: "any",
  })
  async community(
    @graphql.Parent() parent: EntreprisesOnCommunity,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Community | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Community",
    });
    const result = await this.service.getCommunity(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }
}
