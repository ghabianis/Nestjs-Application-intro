/*
//------------------------------------------------------------------------------ 
// This code was generated by Amplication. 
// 
// Changes to this file will be lost if the code is regenerated. 
//
// There are other ways to to customize your code, see this doc to learn more
// https://docs.amplication.com/docs/how-to/custom-code
//
//------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import * as gqlUserRoles from "../../auth/gqlUserRoles.decorator";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { PaginatedInterface } from "../../util/PaginatedInterface";
import { CreateCandidatesOnChatroomArgs } from "./CreateCandidatesOnChatroomArgs";
import { UpdateCandidatesOnChatroomArgs } from "./UpdateCandidatesOnChatroomArgs";
import { DeleteCandidatesOnChatroomArgs } from "./DeleteCandidatesOnChatroomArgs";
import { CandidatesOnChatroomFindManyArgs } from "./CandidatesOnChatroomFindManyArgs";
import { CandidatesOnChatroomFindUniqueArgs } from "./CandidatesOnChatroomFindUniqueArgs";
import { CandidatesOnChatroom } from "./CandidatesOnChatroom";
import { Chatroom } from "../../chatroom/base/Chatroom";
import { Candidate } from "../../candidate/base/Candidate";
import { CandidatesOnChatroomService } from "../candidatesOnChatroom.service";

@graphql.Resolver(() => CandidatesOnChatroom)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class CandidatesOnChatroomResolverBase {
  constructor(
    protected readonly service: CandidatesOnChatroomService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CandidatesOnChatroom",
    action: "read",
    possession: "any",
  })
  async _candidatesOnChatroomsMeta(
    @graphql.Args() args: CandidatesOnChatroomFindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @graphql.Query(() => [CandidatesOnChatroom])
  @nestAccessControl.UseRoles({
    resource: "CandidatesOnChatroom",
    action: "read",
    possession: "any",
  })
  async candidatesOnChatrooms(
    @graphql.Args() args: CandidatesOnChatroomFindManyArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<PaginatedInterface<CandidatesOnChatroom>> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "CandidatesOnChatroom",
    });
    const results = await this.service.findMany(args);
    const result = results.paginatedResult.map((result: CandidatesOnChatroom) =>
      permission.filter(result)
    );
    return { paginatedResult: result, totalCount: results.totalCount };
  }

  @graphql.Query(() => CandidatesOnChatroom, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CandidatesOnChatroom",
    action: "read",
    possession: "own",
  })
  async candidatesOnChatroom(
    @graphql.Args() args: CandidatesOnChatroomFindUniqueArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<CandidatesOnChatroom | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "CandidatesOnChatroom",
    });
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.Mutation(() => CandidatesOnChatroom)
  @nestAccessControl.UseRoles({
    resource: "CandidatesOnChatroom",
    action: "create",
    possession: "any",
  })
  async createCandidatesOnChatroom(
    @graphql.Args() args: CreateCandidatesOnChatroomArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<CandidatesOnChatroom> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "CandidatesOnChatroom",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"CandidatesOnChatroom"} creation is forbidden for roles: ${roles}`
      );
    }
    // @ts-ignore
    return await this.service.create({
      ...args,
      data: {
        ...args.data,

        chatroom: args.data.chatroom
          ? {
              connect: args.data.chatroom,
            }
          : undefined,

        candidate: args.data.candidate
          ? {
              connect: args.data.candidate,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => CandidatesOnChatroom)
  @nestAccessControl.UseRoles({
    resource: "CandidatesOnChatroom",
    action: "update",
    possession: "any",
  })
  async updateCandidatesOnChatroom(
    @graphql.Args() args: UpdateCandidatesOnChatroomArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<CandidatesOnChatroom | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "CandidatesOnChatroom",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"CandidatesOnChatroom"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      // @ts-ignore
      return await this.service.update({
        ...args,
        data: {
          ...args.data,

          chatroom: args.data.chatroom
            ? {
                connect: args.data.chatroom,
              }
            : undefined,

          candidate: args.data.candidate
            ? {
                connect: args.data.candidate,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => CandidatesOnChatroom)
  @nestAccessControl.UseRoles({
    resource: "CandidatesOnChatroom",
    action: "delete",
    possession: "any",
  })
  async deleteCandidatesOnChatroom(
    @graphql.Args() args: DeleteCandidatesOnChatroomArgs
  ): Promise<CandidatesOnChatroom | null> {
    try {
      // @ts-ignore
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Chatroom, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CandidatesOnChatroom",
    action: "read",
    possession: "any",
  })
  async chatroom(
    @graphql.Parent() parent: CandidatesOnChatroom,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Chatroom | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Chatroom",
    });
    const result = await this.service.getChatroom(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.ResolveField(() => Candidate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CandidatesOnChatroom",
    action: "read",
    possession: "any",
  })
  async candidate(
    @graphql.Parent() parent: CandidatesOnChatroom,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Candidate | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Candidate",
    });
    const result = await this.service.getCandidate(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }
}
