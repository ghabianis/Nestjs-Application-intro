/*
//------------------------------------------------------------------------------ 
// This code was generated by Amplication. 
// 
// Changes to this file will be lost if the code is regenerated. 
//
// There are other ways to to customize your code, see this doc to learn more
// https://docs.amplication.com/docs/how-to/custom-code
//
//------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { PaginatedInterface } from "../../util/PaginatedInterface";
import { ContractsOnCandidateService } from "../contractsOnCandidate.service";
import { ContractsOnCandidateCreateInput } from "./ContractsOnCandidateCreateInput";
import { ContractsOnCandidateWhereInput } from "./ContractsOnCandidateWhereInput";
import { ContractsOnCandidateWhereUniqueInput } from "./ContractsOnCandidateWhereUniqueInput";
import { ContractsOnCandidateFindManyArgs } from "./ContractsOnCandidateFindManyArgs";
import { ContractsOnCandidateUpdateInput } from "./ContractsOnCandidateUpdateInput";
import { ContractsOnCandidate } from "./ContractsOnCandidate";
import { Post } from "../../post/base/Post";
import { getListContractsOnCandidateDto } from "./getListContractsOnCandidate.dto";
@swagger.ApiBearerAuth()
export class ContractsOnCandidateControllerBase {
  constructor(
    protected readonly service: ContractsOnCandidateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: ContractsOnCandidate })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: ContractsOnCandidateCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ContractsOnCandidate> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "ContractsOnCandidate",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"ContractsOnCandidate"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        candidate: data.candidate
          ? {
              connect: data.candidate,
            }
          : undefined,

        contract: data.contract
          ? {
              connect: data.contract,
            }
          : undefined,
      },
      select: {
        candidate: {
          select: {
            id: true,
          },
        },

        contract: {
          select: {
            id: true,
            name:true,
          },
        },

        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: getListContractsOnCandidateDto })
  @swagger.ApiForbiddenResponse()
  @swagger.ApiQuery({
    type: () => ContractsOnCandidateFindManyArgs,
    style: "deepObject",
    explode: true,
  })
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<PaginatedInterface<ContractsOnCandidate>> {
    const args = plainToClass(ContractsOnCandidateFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "ContractsOnCandidate",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        candidate: {
          select: {
            id: true,
          },
        },

        contract: {
          select: {
            id: true,
            name:true,
          },
        },

        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    const result = results.paginatedResult.map((result: ContractsOnCandidate) =>
      permission.filter(result)
    );
    return { paginatedResult: result, totalCount: results.totalCount };
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: ContractsOnCandidate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: ContractsOnCandidateWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ContractsOnCandidate | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "ContractsOnCandidate",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        candidate: {
          select: {
            id: true,
          },
        },

        contract: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: ContractsOnCandidate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: ContractsOnCandidateWhereUniqueInput,
    @common.Body()
    data: ContractsOnCandidateUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ContractsOnCandidate | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "ContractsOnCandidate",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"ContractsOnCandidate"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          candidate: data.candidate
            ? {
                connect: data.candidate,
              }
            : undefined,

          contract: data.contract
            ? {
                connect: data.contract,
              }
            : undefined,
        },
        select: {
          candidate: {
            select: {
              id: true,
            },
          },

          contract: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: ContractsOnCandidate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: ContractsOnCandidateWhereUniqueInput
  ): Promise<ContractsOnCandidate | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          candidate: {
            select: {
              id: true,
            },
          },

          contract: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
