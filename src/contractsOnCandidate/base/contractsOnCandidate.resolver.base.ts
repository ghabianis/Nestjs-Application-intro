/*
//------------------------------------------------------------------------------ 
// This code was generated by Amplication. 
// 
// Changes to this file will be lost if the code is regenerated. 
//
// There are other ways to to customize your code, see this doc to learn more
// https://docs.amplication.com/docs/how-to/custom-code
//
//------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import * as gqlUserRoles from "../../auth/gqlUserRoles.decorator";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { PaginatedInterface } from "../../util/PaginatedInterface";
import { CreateContractsOnCandidateArgs } from "./CreateContractsOnCandidateArgs";
import { UpdateContractsOnCandidateArgs } from "./UpdateContractsOnCandidateArgs";
import { DeleteContractsOnCandidateArgs } from "./DeleteContractsOnCandidateArgs";
import { ContractsOnCandidateFindManyArgs } from "./ContractsOnCandidateFindManyArgs";
import { ContractsOnCandidateFindUniqueArgs } from "./ContractsOnCandidateFindUniqueArgs";
import { ContractsOnCandidate } from "./ContractsOnCandidate";
import { Candidate } from "../../candidate/base/Candidate";
import { Contract } from "../../contract/base/Contract";
import { ContractsOnCandidateService } from "../contractsOnCandidate.service";

@graphql.Resolver(() => ContractsOnCandidate)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class ContractsOnCandidateResolverBase {
  constructor(
    protected readonly service: ContractsOnCandidateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "read",
    possession: "any",
  })
  async _contractsOnCandidatesMeta(
    @graphql.Args() args: ContractsOnCandidateFindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @graphql.Query(() => [ContractsOnCandidate])
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "read",
    possession: "any",
  })
  async contractsOnCandidates(
    @graphql.Args() args: ContractsOnCandidateFindManyArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<PaginatedInterface<ContractsOnCandidate>> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "ContractsOnCandidate",
    });
    const results = await this.service.findMany(args);
    const result = results.paginatedResult.map((result: ContractsOnCandidate) =>
      permission.filter(result)
    );
    return { paginatedResult: result, totalCount: results.totalCount };
  }

  @graphql.Query(() => ContractsOnCandidate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "read",
    possession: "own",
  })
  async contractsOnCandidate(
    @graphql.Args() args: ContractsOnCandidateFindUniqueArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<ContractsOnCandidate | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "ContractsOnCandidate",
    });
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.Mutation(() => ContractsOnCandidate)
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "create",
    possession: "any",
  })
  async createContractsOnCandidate(
    @graphql.Args() args: CreateContractsOnCandidateArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<ContractsOnCandidate> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "ContractsOnCandidate",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"ContractsOnCandidate"} creation is forbidden for roles: ${roles}`
      );
    }
    // @ts-ignore
    return await this.service.create({
      ...args,
      data: {
        ...args.data,

        candidate: args.data.candidate
          ? {
              connect: args.data.candidate,
            }
          : undefined,

        contract: args.data.contract
          ? {
              connect: args.data.contract,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => ContractsOnCandidate)
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "update",
    possession: "any",
  })
  async updateContractsOnCandidate(
    @graphql.Args() args: UpdateContractsOnCandidateArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<ContractsOnCandidate | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "ContractsOnCandidate",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"ContractsOnCandidate"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      // @ts-ignore
      return await this.service.update({
        ...args,
        data: {
          ...args.data,

          candidate: args.data.candidate
            ? {
                connect: args.data.candidate,
              }
            : undefined,

          contract: args.data.contract
            ? {
                connect: args.data.contract,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => ContractsOnCandidate)
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "delete",
    possession: "any",
  })
  async deleteContractsOnCandidate(
    @graphql.Args() args: DeleteContractsOnCandidateArgs
  ): Promise<ContractsOnCandidate | null> {
    try {
      // @ts-ignore
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Candidate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "read",
    possession: "any",
  })
  async candidate(
    @graphql.Parent() parent: ContractsOnCandidate,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Candidate | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Candidate",
    });
    const result = await this.service.getCandidate(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.ResolveField(() => Contract, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ContractsOnCandidate",
    action: "read",
    possession: "any",
  })
  async contract(
    @graphql.Parent() parent: ContractsOnCandidate,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Contract | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Contract",
    });
    const result = await this.service.getContract(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }
}
