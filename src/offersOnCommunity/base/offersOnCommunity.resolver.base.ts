/*
//------------------------------------------------------------------------------ 
// This code was generated by Amplication. 
// 
// Changes to this file will be lost if the code is regenerated. 
//
// There are other ways to to customize your code, see this doc to learn more
// https://docs.amplication.com/docs/how-to/custom-code
//
//------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import * as gqlUserRoles from "../../auth/gqlUserRoles.decorator";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { PaginatedInterface } from "../../util/PaginatedInterface";
import { CreateOffersOnCommunityArgs } from "./CreateOffersOnCommunityArgs";
import { UpdateOffersOnCommunityArgs } from "./UpdateOffersOnCommunityArgs";
import { DeleteOffersOnCommunityArgs } from "./DeleteOffersOnCommunityArgs";
import { OffersOnCommunityFindManyArgs } from "./OffersOnCommunityFindManyArgs";
import { OffersOnCommunityFindUniqueArgs } from "./OffersOnCommunityFindUniqueArgs";
import { OffersOnCommunity } from "./OffersOnCommunity";
import { Offer } from "../../offer/base/Offer";
import { Community } from "../../community/base/Community";
import { OffersOnCommunityService } from "../offersOnCommunity.service";

@graphql.Resolver(() => OffersOnCommunity)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class OffersOnCommunityResolverBase {
  constructor(
    protected readonly service: OffersOnCommunityService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "OffersOnCommunity",
    action: "read",
    possession: "any",
  })
  async _offersOnCommunitiesMeta(
    @graphql.Args() args: OffersOnCommunityFindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @graphql.Query(() => [OffersOnCommunity])
  @nestAccessControl.UseRoles({
    resource: "OffersOnCommunity",
    action: "read",
    possession: "any",
  })
  async offersOnCommunities(
    @graphql.Args() args: OffersOnCommunityFindManyArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<PaginatedInterface<OffersOnCommunity>> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "OffersOnCommunity",
    });
    const results = await this.service.findMany(args);
    const result = results.paginatedResult.map((result: OffersOnCommunity) =>
      permission.filter(result)
    );
    return { paginatedResult: result, totalCount: results.totalCount };
  }

  @graphql.Query(() => OffersOnCommunity, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "OffersOnCommunity",
    action: "read",
    possession: "own",
  })
  async offersOnCommunity(
    @graphql.Args() args: OffersOnCommunityFindUniqueArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<OffersOnCommunity | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "OffersOnCommunity",
    });
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.Mutation(() => OffersOnCommunity)
  @nestAccessControl.UseRoles({
    resource: "OffersOnCommunity",
    action: "create",
    possession: "any",
  })
  async createOffersOnCommunity(
    @graphql.Args() args: CreateOffersOnCommunityArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<OffersOnCommunity> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "OffersOnCommunity",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"OffersOnCommunity"} creation is forbidden for roles: ${roles}`
      );
    }
    // @ts-ignore
    return await this.service.create({
      ...args,
      data: {
        ...args.data,

        offer: args.data.offer
          ? {
              connect: args.data.offer,
            }
          : undefined,

        community: args.data.community
          ? {
              connect: args.data.community,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => OffersOnCommunity)
  @nestAccessControl.UseRoles({
    resource: "OffersOnCommunity",
    action: "update",
    possession: "any",
  })
  async updateOffersOnCommunity(
    @graphql.Args() args: UpdateOffersOnCommunityArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<OffersOnCommunity | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "OffersOnCommunity",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"OffersOnCommunity"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      // @ts-ignore
      return await this.service.update({
        ...args,
        data: {
          ...args.data,

          offer: args.data.offer
            ? {
                connect: args.data.offer,
              }
            : undefined,

          community: args.data.community
            ? {
                connect: args.data.community,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => OffersOnCommunity)
  @nestAccessControl.UseRoles({
    resource: "OffersOnCommunity",
    action: "delete",
    possession: "any",
  })
  async deleteOffersOnCommunity(
    @graphql.Args() args: DeleteOffersOnCommunityArgs
  ): Promise<OffersOnCommunity | null> {
    try {
      // @ts-ignore
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Offer, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "OffersOnCommunity",
    action: "read",
    possession: "any",
  })
  async offer(
    @graphql.Parent() parent: OffersOnCommunity,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Offer | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Offer",
    });
    const result = await this.service.getOffer(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.ResolveField(() => Community, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "OffersOnCommunity",
    action: "read",
    possession: "any",
  })
  async community(
    @graphql.Parent() parent: OffersOnCommunity,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Community | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Community",
    });
    const result = await this.service.getCommunity(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }
}
