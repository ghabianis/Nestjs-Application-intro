/*
//------------------------------------------------------------------------------ 
// This code was generated by Amplication. 
// 
// Changes to this file will be lost if the code is regenerated. 
//
// There are other ways to to customize your code, see this doc to learn more
// https://docs.amplication.com/docs/how-to/custom-code
//
//------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { PaginatedInterface } from "../../util/PaginatedInterface";
import { PublicationsOnCommunityService } from "../publicationsOnCommunity.service";
import { PublicationsOnCommunityCreateInput } from "./PublicationsOnCommunityCreateInput";
import { PublicationsOnCommunityWhereInput } from "./PublicationsOnCommunityWhereInput";
import { PublicationsOnCommunityWhereUniqueInput } from "./PublicationsOnCommunityWhereUniqueInput";
import { PublicationsOnCommunityFindManyArgs } from "./PublicationsOnCommunityFindManyArgs";
import { PublicationsOnCommunityUpdateInput } from "./PublicationsOnCommunityUpdateInput";
import { PublicationsOnCommunity } from "./PublicationsOnCommunity";
import { Post } from "../../post/base/Post";
import { getListPublicationsOnCommunityDto } from "./getListPublicationsOnCommunity.dto";
@swagger.ApiBearerAuth()
export class PublicationsOnCommunityControllerBase {
  constructor(
    protected readonly service: PublicationsOnCommunityService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "PublicationsOnCommunity",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: PublicationsOnCommunity })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: PublicationsOnCommunityCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<PublicationsOnCommunity> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "PublicationsOnCommunity",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"PublicationsOnCommunity"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        publication: data.publication
          ? {
              connect: data.publication,
            }
          : undefined,

        community: data.community
          ? {
              connect: data.community,
            }
          : undefined,
      },
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,

        publication: {
          select: {
            id: true,
          },
        },

        community: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "PublicationsOnCommunity",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: getListPublicationsOnCommunityDto })
  @swagger.ApiForbiddenResponse()
  @swagger.ApiQuery({
    type: () => PublicationsOnCommunityFindManyArgs,
    style: "deepObject",
    explode: true,
  })
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<PaginatedInterface<PublicationsOnCommunity>> {
    const args = plainToClass(
      PublicationsOnCommunityFindManyArgs,
      request.query
    );

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "PublicationsOnCommunity",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,

        publication: {
          select: {
            id: true,
          },
        },

        community: {
          select: {
            id: true,
          },
        },
      },
    });
    const result = results.paginatedResult.map(
      (result: PublicationsOnCommunity) => permission.filter(result)
    );
    return { paginatedResult: result, totalCount: results.totalCount };
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "PublicationsOnCommunity",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: PublicationsOnCommunity })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: PublicationsOnCommunityWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<PublicationsOnCommunity | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "PublicationsOnCommunity",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,

        publication: {
          select: {
            id: true,
          },
        },

        community: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "PublicationsOnCommunity",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: PublicationsOnCommunity })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: PublicationsOnCommunityWhereUniqueInput,
    @common.Body()
    data: PublicationsOnCommunityUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<PublicationsOnCommunity | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "PublicationsOnCommunity",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"PublicationsOnCommunity"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          publication: data.publication
            ? {
                connect: data.publication,
              }
            : undefined,

          community: data.community
            ? {
                connect: data.community,
              }
            : undefined,
        },
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,

          publication: {
            select: {
              id: true,
            },
          },

          community: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "PublicationsOnCommunity",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: PublicationsOnCommunity })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: PublicationsOnCommunityWhereUniqueInput
  ): Promise<PublicationsOnCommunity | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,

          publication: {
            select: {
              id: true,
            },
          },

          community: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
